# Sender
vector<string> frames = Utils::getFramesToSend("Hello how are you?")
for (string frame:frames) {
    send(frame)
}

# Receiver
vector<string> frames;
while receiving
    frames.append(frame)
cout << Utils::decodeFrames(frames) << endl;